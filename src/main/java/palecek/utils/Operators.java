package palecek.utils;

public class Operators {
    public static final String REPETITION_OPERATOR = "+"; // indicates that the distance is repeated
    public static final String OPTIONAL_OPERATOR = "?"; // indicates that the distance is optional
    public static final String INF_OPERATOR = "*"; // indicates that the distance is infinite
}
